import { useState } from "react"
import { Text, View } from "react-native"

const Auth = () => {
	const [isReg, setIsReg] = useState(false)
	return (
		<View>
			<Text>Auth</Text>
		</View>
	)
}
export default Auth
